---
import { ArrowRight } from "lucide-react";

interface Props {
  title: string;
  href: string;
}

const { title, href } = Astro.props;
---

<a
  href={href}
  class="group block rounded-xl border border-border bg-card p-5 transition-all hover:border-secondary/30 hover:shadow-sm no-underline"
>
  <div class="flex items-center justify-between gap-2">
    <h3 class="font-semibold text-sm text-primary group-hover:text-secondary transition-colors">{title}</h3>
    <ArrowRight className="size-4 text-muted group-hover:text-secondary group-hover:translate-x-0.5 transition-all shrink-0" strokeWidth={2} />
  </div>
  <div class="text-sm text-muted mt-1.5 leading-relaxed">
    <slot />
  </div>
</a>
