---
import { ShieldCheck, Code, Lock, LayoutGrid, KeyRound, Box } from "lucide-react";
import { features } from "../data/content.ts";

const icons = [ShieldCheck, Code, Lock, LayoutGrid, KeyRound, Box];
---

<section class="relative pb-28 px-6 lg:px-12 mb-16 features-section">
  <div class="max-w-6xl mx-auto pt-28">
    <div class="text-center mb-16">
      <h2 class="text-3xl sm:text-4xl font-extrabold tracking-tight mb-4">Everything you need</h2>
      <p class="text-muted text-lg max-w-xl mx-auto">A complete toolkit for environment variable management, from schema validation to cloud secret resolution. Whether you're building a web app or an autonomous agent.</p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
      {features.map((feature, i) => (
        <div class="glass-card glass-hover rounded-2xl p-7">
          {() => {
            const Icon = icons[i];
            return (
              <div class="w-10 h-10 rounded-xl bg-secondary/10 border border-secondary/10 flex items-center justify-center mb-5">
                <Icon className="w-5 h-5 text-secondary" strokeWidth={1.5} />
              </div>
            );
          }}
          <h3 class="text-primary font-semibold text-base mb-2">{feature.headline}</h3>
          <p class="text-muted text-sm leading-relaxed">{feature.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .features-section {
    clip-path: polygon(0 4rem, 100% 0, 100% 100%, 0 100%);
    background: rgba(255, 255, 255, 0.4);
  }
</style>
