---
title: Built-in Schemas
description: Reference for string, number, URL, boolean, and parsed environment schemas.
order: 4
---

import TypeTable from "@/components/docs/TypeTable.astro";
import Callout from "@/components/docs/Callout.astro";

All built-in schemas are Effect Schemas and may be used directly in a server or
client fragment:

```ts
const appEnv = createEnv(
  server({
    PORT: port,
    DATABASE_URL: redacted(postgresUrl),
  }),
);
```

## Strings

<TypeTable rows={[
  { name: "requiredString", type: "string", description: "A present, non-empty string." },
]} />

Use `optional(schema)` when absence is valid.

## Numbers

<TypeTable rows={[
  { name: "number", type: "number", description: "Any number parsed from a string." },
  { name: "positiveNumber", type: "number", description: "A number greater than zero." },
  { name: "integer", type: "number", description: "A whole number." },
  { name: "nonNegativeNumber", type: "number", description: "A number greater than or equal to zero." },
  { name: "port", type: "number", description: "A whole number from 1 through 65535." },
]} />

```ts
values: {
  PORT: port,
  MAX_RETRIES: integer,
  RATE_LIMIT: positiveNumber,
  THRESHOLD: number,
}
```

## Boolean

<TypeTable rows={[
  { name: "boolean", type: "boolean", description: "Parses true, false, 1, or 0 without case sensitivity." },
]} />

```ts
values: {
  DEBUG: boolean,
}
```

## URLs

<TypeTable rows={[
  { name: "url", type: "string", description: "HTTP or HTTPS URL." },
  { name: "postgresUrl", type: "string", description: "PostgreSQL connection URL." },
  { name: "redisUrl", type: "string", description: "Redis connection URL." },
  { name: "mongoUrl", type: "string", description: "MongoDB connection URL." },
  { name: "mysqlUrl", type: "string", description: "MySQL connection URL." },
  { name: "commaSeparatedUrls", type: "string[]", description: "Comma-separated HTTP and HTTPS URLs." },
]} />

Server connection URLs should normally be redacted:

```ts
values: {
  DATABASE_URL: redacted(postgresUrl),
  REDIS_URL: redacted(redisUrl),
  WEBHOOK_URL: url,
}
```

## Comma-separated values

<TypeTable rows={[
  { name: "commaSeparated", type: "string[]", description: "Trimmed strings separated by commas." },
  { name: "commaSeparatedNumbers", type: "number[]", description: "Numbers separated by commas." },
]} />

```ts
values: {
  ALLOWED_ORIGINS: commaSeparated,
  RETRY_DELAYS: commaSeparatedNumbers,
}
```

## Parameterized schemas

### `stringEnum`

```ts
values: {
  LOG_LEVEL: stringEnum(["debug", "info", "warn", "error"]),
}
```

### `json`

```ts
import { Schema } from "effect";

values: {
  JSON_CONFIG: json(
    Schema.Struct({
      retries: Schema.Number,
      timeout: Schema.Number,
    }),
  ),
}
```

## Effect Schema

Effect Schemas with context requirements are accepted. Schema outputs must not
be functions:

```ts
values: {
  NODE_ENV: Schema.Literal("development", "production", "test"),
  API_KEY: Schema.String.check(Schema.isMinLength(32)),
}
```

<Callout type="tip">
  Use Effect Schema directly when the built-in set does not express the desired
  validation and decoded type. Its errors and context requirements are
  preserved by the resulting environment Effect.
</Callout>
